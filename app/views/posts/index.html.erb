  <div class="posts-block"><% @posts.each do |post| %>
    <div class="post-all">
      <h2><%= link_to post.title, post_path(post.id), class: "post-title"%></h2>
      <p class="post-body"><%= get_limited_version(post.body) %><p>
    </div>
  <% end %></div>
